package com.tax;

public class Parser {
    private Basket basket;

    public Parser(Basket basket) {
        this.basket = basket;
    }

    public boolean parse(String item) {
        return true;
    }
}
